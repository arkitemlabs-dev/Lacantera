rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ⚠️ REGLAS TEMPORALES PARA DESARROLLO
    // TODO: Restringir en producción
    
    match /{document=**} {
      // Permitir todo por ahora (solo desarrollo)
      allow read, write: if true;
    }
  }
}
